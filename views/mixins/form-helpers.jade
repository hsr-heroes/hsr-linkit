//- Todo: Use some mixins to more easily build forms
mixin field(type, name, label, placeholder)
    .field(class='field-' + type)
        label #{label}:
        input(type=type, name='user[#{name}]', value=user[name], placeholder=placeholder)